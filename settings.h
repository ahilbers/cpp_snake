#pragma once

namespace settings {

  // // constants across the game -- define in .h file, no associated .cpp
  // int pause = 100000;  // pause between steps, microseconds
  // const int map_side_length = 13;  // keep odd so map as middle
  extern const int pause;
  extern const int map_side_length;

}
